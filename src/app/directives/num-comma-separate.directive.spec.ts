import { NumCommaSeparateDirective } from './num-comma-separate.directive';

describe('NumCommaSeparateDirective', () => {
  it('should create an instance', () => {
    const directive = new NumCommaSeparateDirective();
    expect(directive).toBeTruthy();
  });
});
